Telemini 除錯回覆修正包

覆蓋檔案：
- services/ai_actions.py
- handlers/call_handler.py

修正內容：
1. 🗣️ 除錯回覆不再直接補送按鈕綁定的舊 AI action。
2. 🗣️ 改成走 /reply 救援邏輯，以目前聊天室最後一筆短期記憶為準。
3. 如果最後一筆短期記憶是 user：
   - 使用那一句 user 內容重新呼叫 Gemini 補回覆。
   - 不重複寫入 user 記憶。
4. 如果最後一筆短期記憶是 assistant：
   - 直接把最後一筆 AI 回覆重送到聊天室。
   - 不新增 chat_memory。
5. /hidden Reply Keyboard 的 🗣️ 與 AI 訊息下方 Inline Keyboard 的 🗣️ 都修正。
6. 安全阻擋提示的 🗣️ 原本已走 run_reply_recovery，本包保留。

已檢查：
python -m py_compile services/ai_actions.py handlers/call_handler.py
