Telemini blocked-button + summary-repair patch

覆蓋檔案：
- handlers/message_handler.py
- handlers/call_handler.py
- services/ai_actions.py
- services/call_ai.py
- services/memory_summary.py

更新內容：
1. 一般安全阻擋提示改成按鈕：
   - 訊息只顯示「內容被安全阻擋」
   - 下方出現「🗣️ 嘗試重跑回覆」
   - 點擊後會刪除阻擋提示訊息
   - 然後執行 /reply 救援式重跑

2. AI 操作流程被阻擋也改成按鈕：
   - 重跑被擋：顯示「🗣️ 重新產生」
   - 接續被擋：顯示「🗣️ 重新接續」
   - 點擊後會刪除阻擋提示訊息

3. 長期摘要阻擋提示改成按鈕：
   - 訊息顯示「摘要長期記憶時被阻擋」
   - 下方出現「清理後重跑摘要」
   - 點擊後會刪除提示訊息
   - 依阻擋階段清理可能半完成資料，再跑手動摘要

4. 摘要修復會依階段處理：
   - segment：分段摘要阻擋，重算摘要進度
   - state：目前狀態摘要阻擋，刪除最新分段摘要與目前狀態後重跑
   - archive：封存摘要阻擋，刪除最新封存並解除來源摘要封存
   - archive_merge：封存合併阻擋，清理快取與摘要進度後重跑

5. 順手修復目前最新 zip 裡的殘留問題：
   - handlers/message_handler.py 補回 /setting /memory /hidden /reply 指令分支
   - handlers/call_handler.py 移除多餘右括號
   - services/ai_actions.py 移除多餘右括號

已檢查：
python -m py_compile handlers/message_handler.py handlers/call_handler.py services/ai_actions.py services/call_ai.py services/memory_summary.py
