Telemini 移除「目前狀態」記憶引入修正包

覆蓋檔案：
- services/style.py

修改內容：
- 只移除 _build_memory_context_text() 中把 memory_context.state 加入 prompt 的區塊。
- 也就是不再把「【目前狀態】」送進 Gemini prompt。
- 其他 prompt 文字、BASE_STYLE、RESPONSE_RULES、聊天/劇場風格、摘要與封存摘要都沒有改。

注意：
- 這包不會刪除資料庫既有 memory_summary_state/current_state 資料。
- 只是讓「目前狀態」不再被引入主遊戲 prompt。
